- name: Setup my workplace
  hosts: controlnodes

  pre_tasks:
    - name: Get user
      ansible.builtin.shell: whoami
      register: user
    - name: Get user id
      ansible.builtin.shell: id -u
      register: user_id

  roles: 
    - role: common
    - role: docker
