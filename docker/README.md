## Enable Github Copilot
Open neovim, `:LazyExtras` and add `ai.copilot` (also add `ai.copilotchat` for enabling a chat window within neovim). Now, go to `Lazy` and install the added plugins. Next, restart Lazyvim, type Copilot auth and authenticate yourself. If this works, Copilot should be available.
